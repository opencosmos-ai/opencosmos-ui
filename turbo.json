{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": [
    "CACHE_BUST"
  ],
  "globalPassThroughEnv": [
    "EDGE_CONFIG",
    "EDGE_CONFIG_ID",
    "ADMIN_API_KEY"
  ],
  "tasks": {
    "build": {
      "dependsOn": [
        "^build"
      ],
      "outputs": [
        ".next/**",
        "dist/**"
      ]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "lint": {},
    "typecheck": {
      "dependsOn": ["^build"]
    },
    "test": {
      "dependsOn": ["^build"]
    },
    "size:check": {
      "dependsOn": ["build"]
    }
  }
}
